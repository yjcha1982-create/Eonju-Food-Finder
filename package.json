{
  "name": "lunch-picker",
  "version": "1.0.0",
  "private": true,
  "description": "500m 주변 식당을 검색하고 무작위로 점심을 고르는 웹앱",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
